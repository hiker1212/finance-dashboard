PORT=3300
DB_PATH=./data.db
ANTHROPIC_API_KEY=your_api_key_here
# Set to the built client directory to serve the SPA from Express (production only)
# STATIC_PATH=/app/public
# Restrict CORS to your domain in production
# CLIENT_ORIGIN=http://localhost:3001

